name: Android 11 x64 Live 1 Hour VM

on:
  workflow_dispatch:

jobs:
  run-1hour-android11:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install QEMU, noVNC, and Utilities
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 qemu-utils wget adb novnc websockify

      - name: Download Android 11 x64 ISO from Archive.org
        run: |
          echo "Pulling clean direct ISO payload from Internet Archive servers..."
          # Double quotes handle the URL spacing variables flawlessly
          curl -L -A "Mozilla/5.0" \
            -o "Android 11 x64 (User).iso" \
            "https://archive.org/download/androidx86-11-isos/Android%2011%20x64%20%28User%29.iso"

      - name: Verify ISO File Identity and Size
        run: |
          echo "Verifying downloaded raw storage allocation data..."
          ls -lh "Android 11 x64 (User).iso"

      - name: Provision Storage Drive
        run: |
          qemu-img create -f qcow2 android11_disk.qcow2 16G

      - name: Launch Android 11 in QEMU (IDE CD Emulation Mode)
        run: |
          echo "Starting QEMU container..."
          sudo qemu-system-x86_64 \
            -enable-kvm \
            -cpu host \
            -m 4G \
            -smp 2 \
            -drive file=android11_disk.qcow2,format=qcow2,index=0,media=disk \
            -drive file="Android 11 x64 (User).iso",index=1,media=cdrom,format=raw \
            -boot order=d \
            -vga virtio \
            -display none \
            -vnc 127.0.0.1:0 \
            -net nic,model=virtio \
            -net user,hostfwd=tcp::5555-:5555 \
            -monitor telnet:127.0.0.1:4444,server,nowait \
            -daemonize

      - name: Skip GRUB Boot Menu Automatically
        run: |
          echo "Waiting 12 seconds for the Android 11 boot options layout to display..."
          sleep 12
          
          echo "Injecting return key over clean network port using Python..."
          python3 -c '
          import socket, time
          s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
          s.connect(("127.0.0.1", 4444))
          time.sleep(0.5)
          s.sendall(b"\n\nsendkey ret\n")
          time.sleep(0.5)
          s.close()
          '
          echo "Boot command executed successfully."

      - name: Start Websockify (VNC to WebSockets)
        run: |
          websockify --web /usr/share/novnc/ 6080 127.0.0.1:5900 &
          sleep 2

      - name: Generate Public Remote Link and Hold for 1 Hour
        run: |
          echo "================================================================"
          echo "LAUNCHING AUTHENTICATED TUNNEL ENGINE OVER PORT 443..."
          echo "================================================================"
          
          # Stable web tunnel utilizing your custom registered profile token ID parameter
          ssh -o StrictHostKeyChecking=no -p 443 -R 80:localhost:6080 RPLfy23ckPE+80@a.pinggy.io > pinggy.log 2>&1 &
          
          sleep 10
          
          echo "================================================================"
          echo "--> YOUR ONLINE VIRTUAL MACHINE REMOTE DETAILS <--"
          echo "================================================================"
          
          # Print out your official custom secure URL address link
          grep -o 'https://[^ ]*\(pinggy.link\|pinggy.io\)' pinggy.log || cat pinggy.log
          
          echo ""
          echo "INSTRUCTIONS:"
          echo "1. Click the .pinggy.link URL printed directly above."
          echo "2. On the web page that opens, click on 'vnc.html'."
          echo "3. Use your computer keyboard ARROW KEYS to steer Up/Down/Left/Right!"
          echo "================================================================"
          
          sleep 3600
